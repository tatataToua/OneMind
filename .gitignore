# Python
backend/.venv/
backend/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node
frontend/node_modules/
frontend/dist/

# Local env overrides
.env
backend/.env

# Eval scratch. `./run.ps1 eval` writes this for a quick single-arm check;
# the committed evidence is evals/comparison_report.json, which the README
# quotes from and which `./run.ps1 compare` regenerates.
evals/report.json

# Agent tooling. Local machine setup, not project source: the BMAD module
# install and the vendored Claude skill packages. Both are already excluded
# from every deploy (.dockerignore, .gcloudignore); this keeps them out of the
# repo a reviewer clones too.
.claude/skills/
_bmad/

# OS
.DS_Store
Thumbs.db
frontend/*.tsbuildinfo
